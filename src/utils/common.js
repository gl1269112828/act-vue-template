const commonFn = class {
  calculateDeDuplication(arr, brr) {
    let crr = [];
    for (let i = 0; i < arr.length; i++) {
      let itemI = arr[i];
      console.log(item);
      for (let j = 0; j < brr.length; j++) {
        let itemJ = brr[j];
        if(item === itemJ)
        console.log(itemJ);
      }
    }
    // console.log(arr);
    // console.log(brr);
  }
};
export default commonFn;
